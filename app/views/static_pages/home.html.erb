<h1 class="header">Welcome to Fumblr!</h1>
<% if user_signed_in? %>
  <%= "Hello #{current_user.email}" %>
  <%= render @posts %>
<% else %>
  <p><%= link_to "Sign In", new_user_session_path %> or <%= link_to "Sign Up", new_user_registration_path %> to follow users and post!</p>
<% end %>